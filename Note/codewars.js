'use strict';
function formatDuration(seconds) {
    seconds;
}
const hours = Math.floor(3662 % 3600);
const minutes = Math.floor(hours / 60);
const seconds = Math.floor(hours % 60);
console.log(hours, minutes, seconds);
